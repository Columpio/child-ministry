import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import{BookOpen,ChevronLeft,ChevronRight,Home,Star,CheckCircle}from'lucide-react';import'./style.css';
function App(){const[data,setData]=useState([]),[lesson,setLesson]=useState(null),[slide,setSlide]=useState(0),[answers,setAnswers]=useState({}),[checked,setChecked]=useState(false);useEffect(()=>{fetch('/generated/course.json').then(r=>r.json()).then(setData)},[]);const lessons=data[0]?.lessons||[],l=lesson===null?null:lessons[lesson],s=l?.slides?.[slide],total=l?.slides?.length||0;const goNext=()=>{if(slide<total-1){setSlide(slide+1);setChecked(false)}else setLesson(null)};if(!l)return <><Header/><main><div className="welcome"><div className="spark">✦</div><h1>Путешествие по Библии</h1><p>Открывай истории, смотри видео, изучай слайды и отвечай на вопросы!</p></div><h2>Курс «{data[0]?.title||'Книга Бытие'}»</h2><div className="grid">{lessons.map((x,i)=><button className={`lesson ${!x.slides.length?'lesson-disabled':''}`} key={x.id} disabled={!x.slides.length} onClick={()=>{setLesson(i);setSlide(0);setAnswers({})}}><span className="num">{String(i+1).padStart(2,'0')}</span><strong>{x.title}</strong><small>{x.slides.length?`${x.slides.length} слайдов`:'Материалы готовятся'}</small><Star className="star"/></button>)}</div></main></>;return <><Header onHome={()=>setLesson(null)}/><main><div className="crumb" onClick={()=>setLesson(null)}><Home size={15}/> Все уроки <ChevronRight size={15}/>{l.title}</div><section className="hero"><div><small>УРОК {String(lesson+1).padStart(2,'0')} · {data[0].title.toUpperCase()}</small><h1>{s?.title||l.title}</h1><p>Слайд {slide+1} из {total}</p></div><div className="progress"><i style={{width:`${(slide+1)/total*100}%`}}/></div></section><div className="media"><div className="video">{s?.video_url?<iframe src={s.video_url} title="Видео урока" allow="autoplay" allowFullScreen/>:<div className="missing">🎬<b>Видео пока не добавлено</b></div>}</div><div className="slide">{s?.presentation_embed_url?<iframe src={`${s.presentation_embed_url}${s.presentation_embed_url.includes('?')?'&':'?'}rm=minimal`} title="Слайд презентации"/>:s?.image?<img src={s.image} alt={s.title}/>:<div className="missing">🖼️<b>Слайд пока не добавлен</b></div>}</div></div><section className="questions"><div className="questionHead"><span>💡</span><h2>Вопросы к слайду</h2></div>{(s?.questions||[]).map((q,i)=><div className="q" key={i}><label>{q.prompt}</label>{q.options?.map((o,j)=><label className="option" key={j}><input type="radio" name={`q-${slide}-${i}`} checked={answers[`${slide}-${i}`]===j} onChange={()=>{setAnswers({...answers,[`${slide}-${i}`]:j});setChecked(false)}}/>{o.text}</label>)}</div>)}{checked&&<p className="result">Ответы сохранены! Продолжай изучать Божье Слово ✨</p>}<button className="check" onClick={()=>{setChecked(true);setTimeout(goNext,500)}}><CheckCircle size={18}/> {slide===total-1?'Завершить урок':'Сохранить и дальше'}</button><div className="nav"><button disabled={!slide} onClick={()=>{setSlide(slide-1);setChecked(false)}}><ChevronLeft/> Назад</button><button onClick={goNext}>Следующий <ChevronRight/></button></div></section></main></>};function Header(p){return <header><BookOpen/><b>Книга Бытие</b><span>Учимся вместе ✨</span>{p.onHome&&<button onClick={p.onHome}><Home size={18}/> К урокам</button>}</header>}createRoot(document.getElementById('root')).render(<App/>);